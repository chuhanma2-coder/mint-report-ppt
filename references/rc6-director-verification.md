# 设计导演执行修复：验收记录

代码：`0.4.0-rc.6-design-dev.9`。运行指纹：`07a0f8cae6a243bb56bf8ff4fcbff22f6d467f0f12e7f67815704a502c565072`。

这是按用户授权推送正式仓库的预发布代码，不是“全部视觉问题已解决”的声明。保留已发布 `v0.4.0-rc.6`，不移动旧标签，不自动替换已安装 Skill。原始业务材料、测试PPT及宿主会话日志仅保存在本地，不随公开发布上传。

## 本轮针对的问题与实际改动

| 问题 | 最小修复 | 验证 |
|---|---|---|
| 设计写了但组件没执行 | 使用现有六种 treatment 的有限词表；验证真实文字/指标/表格/关系，不只检查回显属性 | 未知 treatment、无 P0 实体指标、类型错配的反例 |
| 空白来自固定 band 高度 | 取消固定百分比空行，保留列比例、记录建议 share 与浏览器自然高度 | 浏览器候选、短表自然尺寸、ink-only dead-zone 反例 |
| 候选只是换 padding | 现有 profile/path 的内部重组；完整替代构图与局部修复分开计数 | `director-content-first` 的真实 DOM 差异；等价拓扑不能证明必须拆页 |
| 拆页证明不完整 | 最终相邻分片完整并集合页实测，记录 `merge-recheck`；拒绝未测量/缺分片回执 | 真实容量测试与负例 |
| 看起来连接，PPT对象没有连接 | native connector 使用真实业务节点 ID；回流分段共享同一折点对象 | 共享节点/回流测试；虚构端点与含斜杠 ID 的反例 |
| 漏斗/重点数字被载体吞掉 | 同总体递减阶段条/漏斗与转化率；类目指标字号、字重、颜色绑定到测量图形及原生对象 | 同总体/零分母/精确值/原生绑定测试 |
| 明细自动等于表格 | 删除“明细”关键词直接触发表格的捷径 | 同表格输入按排序、变化、核对目的路由 |
| 两阶段 preflight 假门禁或误报 | 建立设计阶段 receipt，允许后续 copy review，设计/源数据/代码变动则失效 | 无预案/过期预案与正常两阶段用例 |
| 生成 Agent 自填独立 PASS | 封存源文件、IR、代码、PPT、QA和渲染图；不同宿主线程的真实图片结果与固定日志前缀 | 假路径提及、未完成看图、后补图片、材料/图片/QA篡改负例 |
| 删除 sidecar 绕过最终检查 | 生成 PPT 自带 review-required 标记；最终审计重新验证 evidence/receipt | 无独立复核的 CLI 构建不能交付 |
| 人工改稿被旧 IR 绑架 | 显式人工最终版审计只看当前 PPT，不恢复旧数据；发布始终读真实页序 | 模拟增删改排后的原生 PPT→HTML 测试 |
| P1数字比P0更大 | 修正普通scalar metric的较大默认字号；profile按指标优先级使用既定Token | 浏览器反例先失败，修复后实际字号P0>P1 |
| 无指标对象仍保留空指标列 | 同一profile内部让真实说明占用释放的列，不补造指标 | 原语实测与肯尼亚受控重编译 |
| 图表正文没进入页面 | Chart/Image消费已有displayCopy.text；Chart原生和形状路径均编译说明 | Canvas及最终OOXML文字断言 |
| 长单位超出固定标签框 | 横向图形按数值及单位保留标签宽度，不删除单位或全页缩字 | 长单位、两系列颜色、标签贴近数据点回归 |
| 稀疏关系图只剩节点名 | 所有拓扑消费同一完整节点正文；孤立及单边节点保留原生fact-target | 三类节点正文、最终对象定位测试及B材料原生事实核对 |
| 自然拆表后误报表达丢失 | 分片携带originModuleId，逐片验证表达，不以名称后缀冒充继承关系 | 分片表达错配/伪造后缀反例；真实B材料通过 |
| 正确数字被覆盖检查误拒绝 | 完整数值token比较；保护相邻单元格边界及文件名数字 | 10.0/10、6,36、相邻0.9与0.1、文件扩展名回归 |
| 选完分页后才发现主次/空白失败 | 同一测量门禁在候选选择前执行，拒绝无局部主焦点的续页 | 稀疏样例先阻断；正文P0/P1实测断言 |

## 自动化回归

已在上述指纹的代码上通过：

- `npm test`（包括源事实、Presentation Copy、Director、真实浏览器/native、原生六种图表、可编辑形状图表和最终表格）。
- `npm run test:publication`（使用捆绑渲染器，不是 Windows PowerPoint）。
- `test:rc5-topology`、`test:rc5-capacity`、`test:rc5-primitives`、`test:rc5-scenes`：同一最终指纹通过。十个合成场景不是十次Fresh Planner测试。

独立代码审查发现的五项实质问题已修复：来源 `path` 键、假看图字符串、preflight/copy 两阶段哈希冲突、斜杠 edge ID、缺分片合页回执。对应反例进入测试。宿主回执是本地证据核对，不宣称是密码学签名或无法伪造的安全证明。

## 真实材料与发布门槛

首次B/C/肯尼亚短Prompt均从原始材料独立规划；开发期失败记录完整保留。之后的冻结版本重试复用当次源模型/IR，明确计为受控回归，**不计为同一最终版本的新Fresh Run**。B当前为5页原生技术候选，肯尼亚为1页；C仍未完成可交付PPT。不能据此宣布普通短Prompt已稳定达到带教水平。

| 门槛 | 当前状态 |
|---|---|
| B/C/肯尼亚短 Prompt 首轮独立生成 | 开发初始首轮已执行并暴露缺陷；不是最终指纹全部通过 |
| B最终代码受控重编译 | 5页，内容/原生/技术通过；独立视觉87/72/66/78/73，章均75.2，FAIL；最终交付审计正确阻断 |
| 肯尼亚最终代码受控重编译 | 1页，7/7来源单元原生可见；独立视觉92/100，Content/Technical/Design及绑定回执审计PASS，Windows未验证 |
| C真实多页材料 | 旧冻结包7页Canvas未通过设计；最终包修正两处组件审阅映射后11/11可见事实通过，但多载体用途说明缺失触发`EVIDENCE_DUPLICATION`，未进入Canvas，不存在已验收原生PPT |
| 各组第二次独立生成 | NOT RUN，未用重编译冒充 |
| 带教详细 Prompt 对照 | PENDING |
| 每页 ≥85、全章均分 ≥90、短长差距 ≤10 | 肯尼亚受控候选达到页/章门槛；B未达到；短长Prompt差距未验证 |
| 真实多页章节逐事实/逐页审阅 | B全部5页独立审阅完成但未通过；C阻断，未形成已验收Golden Benchmark |
| Windows PowerPoint 打开、编辑、保存、重开 | NOT TESTED — 无真实环境证据 |
| 正式跨平台发布资格 | NOT APPROVED |

肯尼亚前一冻结包独立复核88/100，修复后新PPT、新封存证据及新的独立审阅为92/100；不是修改旧评分。B仍有第2页MVP断词、窄表拆散数字范围，第3页近半页空白且来源/结论等重，第4–5页重复标题和路径内部层级不足；另有指令式/口语文案。独立审阅列出9项开放问题，归属Planner或Canvas，未冒称已修复。B最终审计同时拒绝了一项不符合格式的容量证据引用，不能据此宣称所有审阅证据均已闭环。

上述6张当前原生渲染图均在独立宿主上下文实际查看，真实看图回执通过；审阅与回执共约10分02秒（两章共享一次上下文，不重复相加）。这证明独立门禁可以拒绝技术通过但视觉不合格的页面，不证明默认设计质量已在所有材料上解决。

计时按原始记录保存：B最终构建仪表约63.8秒，肯尼亚约27.6秒；这不是端到端Planner耗时。期间多次冻结重试、源模型及作者修复不计作首次生成速度，也不能用构建秒数宣称12分钟目标已验证。

“verified-release” 是运行包与封存指纹相符，不等于视觉或平台验收通过。技术通过、内容通过、独立视觉通过和 PowerPoint 实测必须分别报告；不得用测试数量、版本号或旧 IR 重编译替代真实首轮生成结果。
