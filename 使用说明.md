# Mint Report PPT 使用说明

> 本文对应设计导演修复标签 `v0.4.0-rc.6-design-dev.9`，验收状态见 [当前记录](references/rc6-director-verification.md)。旧 `v0.4.0-rc.6` 标签保持不变，不包含本轮修复；更新安装需要显式执行下面的安装步骤。

适用版本：**v0.4.0-rc.6-design-dev.9 预发布版**。代码发布不代表 Windows 和全部视觉场景均已验收。生成后仍需查看实际 PPT；不要只看“检查通过”的文字报告。

rc.6 默认先写面向领导的展示文案，再生成对象；不再直接打印来源字段和视觉关键词。无需额外加长 Prompt。中文优先、保留正式名称；完整事实可以改写成短句、指标和注释，不等于逐字搬原文。

## 先回答三个问题

- **需要先优化 Prompt 吗？不需要。** 提供原始材料、汇报目的和制作范围即可。你有明确的页数、风格、强调对象时再补充，不必先让另一个 AI 重写一遍。
- **任务卡必须有吗？底层生成流程需要，但不一定要你单独制作。** 一个人独立制作，可以让 Agent 在同一条任务中自动创建内部任务卡并继续出 PPT；多人分工，先生成一份统一任务卡，所有人使用同一份。
- **必须填写 sectionId 吗？通常不用。** 写负责人姓名即可选中该负责人全部正式章节。只做其中一部分时，写原始大纲名称或编号；Agent应匹配正式 ID，真正有歧义再询问。

普通流程：**原始材料 → 可编辑 PPT → 在 PowerPoint 中修改 → 人工合并（如需要）→ 最终 PPT → 只读 HTML（如需要）**。

## 0．安装或更新一次

在允许安装 Skill 的对话中粘贴：

```text
请从 https://github.com/chuhanma2-coder/mint-report-ppt 安装或更新
mint-report-ppt 到精确标签 v0.4.0-rc.6-design-dev.9，不使用会变化的 main 代替。

先备份现有安装，不修改 mint-report-html 和 mint-report-deck。
只安装运行文件及当前合同，不把历史测试结果和实现进度作为 Skill 运行资料。
安装后报告实际路径、VERSION、Git 提交和运行代码指纹；指纹不匹配时先停止制作并说明原因。
检查 Presentations 原生运行能力、浏览器和中文字体；缺少依赖时明确告诉我。
```

安装需要当前环境具备读取仓库、写入 Skill 安装目录和加载依赖的权限；仅在消息中写版本号不会自动更新本地文件。此版本不要求安装 `mint-report-html`。Presentations 是内部创建/渲染依赖，用户不必在每步另写一个长 Prompt。

Windows 可从独立解压目录运行 `scripts/install-windows.ps1`；该脚本会备份现有版本并检查 PowerPoint、Chrome/Edge 和微软雅黑。Mac 可由 Agent用 `scripts/package-skill.mjs` 制作运行包后备份安装。不能在正式安装目录里临时修改代码后继续声称“原版通过”。

## 1A．一个人制作：一条 Prompt 即可

附上原始 Word、Excel、图片等材料，然后粘贴：

```text
请使用 $mint-report-ppt，精确版本 v0.4.0-rc.6-design-dev.9。
根据本次附件制作面向管理层的可编辑 PPT，帮助读者理解主要进展、关键问题和下一步。

这是我独立负责的报告。如没有任务卡，请在内部创建当前版本任务卡并直接继续，不必让我另跑任务卡步骤。
先完整读取材料，以管理问题组织内容；同一决策的结论、证据和行动尽量集中，只有真实测量放不下才自然续页。
全部业务事实进入可读正文，保留数值、单位、主体、预测、条件和范围；不放到备注或附录，不联网补充业务事实。
图表辅助说明，不机械堆表格、卡片和重复说明；检查实际 PPT 的内容、关系、字号和页面效果。
交付一份原生可编辑的当前版 PPTX，不覆盖旧文件。没有通过的验收项如实说明。
```

如果目的不是进展汇报，把“帮助读者……”这一句改为真实目的，例如“比较三个方案并决定先推进哪个”。其余无需自行润色。

## 1B．多人分工：统筹人先生成统一任务卡

附上或粘贴完整大纲、负责人分工和必要材料：

```text
请使用 $mint-report-ppt，精确版本 v0.4.0-rc.6-design-dev.9。
根据本次提供的大纲和分工，只生成 report.mint-ppt-task.json，不生成 PPT 或 HTML。
保留原始编号、名称、顺序、业务内容和负责人，不补写缺失编号。
每位负责人可以负责多个正式章节，自动汇总为 Work Package，不新造合并章节 ID。
记录当前 Skill、主题、母版版本和汇报目的；所有业务事实默认进入正文，不允许附录，不预先固定页数。
分工真正不明确时先询问。完成后简短列出每个人的负责范围。
```

将同一份任务卡及各自原始材料发给负责人。二次更新任务卡由统筹人统一完成；不要让大家各自改版本、章节 ID 或哈希。

## 2．各负责人并行制作

每位负责人开启自己的制作对话，附统一任务卡和本人材料。只改下面的姓名：

```text
请使用 $mint-report-ppt，精确版本 v0.4.0-rc.6-design-dev.9。
读取附件中的统一任务卡和原始材料，制作【刘屹】负责的全部正式章节，不制作其他负责人的内容。
同一负责人有多个章节时一起完成，保持任务卡中的正式章节身份和顺序，不需要我手填 sectionId。
先完整理解材料及管理问题，再选择表达和测量排版。一个决策的风险、证据和应对不要机械拆成多页。
全部独有事实进入可读正文，不删条件、单位或预测属性，不放到备注或附录。
检查最终 PPT 的内容完整性、实际关系、字号、留白、主次和碰撞。
交付“负责人-当前版.pptx”，不覆盖已有文件；明确说明尚未通过的项目。
```

只做部分章节时加一句：“这次只做大纲‘一、肯尼亚……’，不制作我负责的其他章节。”显式范围优先于整位负责人范围。

**旧版任务卡：**先让统筹人“保留全部业务内容、负责人及正式章节 ID，仅升级到当前版本并重新计算校验”，确认范围差异后再发给大家。不要直接手改 VERSION 或哈希。

## 3．可选：先看规划，避免等成品才发现方向错了

在生成前粘贴：

```text
请使用 $mint-report-ppt，v0.4.0-rc.6-design-dev.9，采用 preflightMode=preview。
读取本次任务卡和原始材料，只给我一份简短的执行预览：负责范围、管理决策、核心结论及证据、需要保留的时间/并行/条件关系、建议如何集中成页、待确认事项。
现在不要生成 PPT，不先承诺未经测量的页数。
```

确认后回复：“按刚才确认的范围和目的继续生成。保留原始材料为事实依据，执行当前版本的真实排版与最终检查。”此预览不是必须步骤。

如果确实只能一页，加：“**必须单页且全部事实可读；不能同时满足时，报告具体冲突，不偷偷删减、缩字或拆页。**”不要同时要求“绝不拆页”和“放不下可自行拆页”。

## 4．修改：优先直接在 PowerPoint 中改

改文字、数字、图片、位置、增删页，可以人工直接操作，无需 Agent。需要 Agent 帮忙时，上传**最新修改后的 PPT**：

```text
请使用 $mint-report-ppt。以本次上传的当前 PPT 为唯一工作文件，不从旧 HTML、IR 或历史稿重建。
只修改我下面列出的地方，保留其他人工修改：
【在这里写具体修改】
修改后检查受影响页；如涉及标题、增删页或合拆页，同时检查相邻页和标题链。
另存一个新版本，不覆盖我上传的原文件。
```

若要从材料重新设计而非保留人工编辑，明确说“从原始材料重新规划，不沿用旧稿的分页和布局，另存新文件”。普通重做不强制新开对话；正式公平测试才需要独立干净工作区。

## 5．合并：人工即可，Agent不是必需步骤

Mac、Windows 都可在 PowerPoint 中插入/复制完整幻灯片并保留源格式。按任务卡中的章节顺序排列；同一负责人负责不相邻章节时，不要把整份负责人文件直接接在末尾导致顺序错误。

合并后检查字体、对象、页序及人工修改。Windows 原生自动合并脚本是可选工具，不是整个协作流程必须依赖 Windows 的原因。不引入 Mac 自动合并工程。

## 6．定稿后发布 HTML（可选）

上传人工修改、合并完成的最终 PPT：

```text
请使用 $mint-report-ppt，将本次上传的最终 PPT 发布为一个离线只读 HTML。
只读取最终 PPT 真实存在的全部页面及实际顺序；保留我的增删页、数字和对象调整。
不从旧任务卡、IR 或源材料恢复内容，不重新排版或改写。
检查 HTML 页数和顺序与最终 PPT 一致，并实际打开检查。不要把内部排版 HTML 当成最终发布文件。
```

当前发布 HTML 以最终 PPT 的渲染页面展示，不是可编辑网页，也不能保证文字可选中。需要 PDF 时，通过 PowerPoint 自带功能导出即可。

## 7．验收时看什么

先打开 PPT，不以 Agent 的“全部通过”代替查看：

1. 原材料中的数字、单位、主体、条件、预测和独有说明是否都能在正文读到。
2. 时间窗口有没有变成确定日期，并行有没有画成串行，集团数字有没有贴在子公司旁边。
3. 同一管理问题是否被无故拆散；图表/表格是否过大；有没有可用却闲置的大块空间。
4. 标题、重点和证据是否对应，正常阅读时文字是否清楚；能否实际编辑原生对象。
5. 保存、关闭、重新打开后是否正常。领导使用 Windows，应在领导环境做这一步。
6. 文字是否像给人阅读的汇报：没有竖线字段串、视觉关键词、无必要英文标签；名称、定位、核心指标和简明说明层级清楚。Human Presentation Copy 未通过，不能报告 DESIGN PASS。

可粘贴：

```text
请复核本次最终 PPT，而非只检查 IR 或内部预览。
分别报告：来源结构覆盖、事实组成核对、语义关系、实际视觉、原生对象、目标平台验证。
列出具体未通过页和问题，不用一个覆盖百分比代替全部验收。
同时报告实际版本、代码指纹、页数、修复次数及可观测耗时；未知耗时写未知。
```

## 当前候选版的边界

rc.5 已加入局部实测容量修复、网络/共享节点/回流、对象级事实定位，以及更严格的跨页和视觉审阅要求。候选代码推送不等于所有实际材料已达到视觉接受标准。六次独立 Fresh Benchmark 的合格重测、真实多页 Golden Benchmark 与 Windows PowerPoint 最终验收须分别有真实证据，不能拿合成样例或旧 IR 重编译替代。
