interface:
  display_name: "Mint Report PPT"
  short_description: "原生PPT协作编辑，最终发布只读HTML"
  brand_color: "#00866A"
  default_prompt: "Use $mint-report-ppt. Read all supplied source material, create a source-grounded native editable section PPTX under the shared task card, and do not generate draft HTML. After PowerPoint review, merge the current PPTX files without re-layout and publish the actual final PPT pages as one offline read-only HTML."
policy:
  allow_implicit_invocation: true
