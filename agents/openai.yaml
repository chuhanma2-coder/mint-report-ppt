interface:
  display_name: "Mint Report PPT"
  short_description: "原生PPT协作编辑，最终发布只读HTML"
  brand_color: "#00866A"
  default_prompt: "Use $mint-report-ppt to create my assigned PPT sections: read all sources, establish the audience and management story, preserve every body fact, express important relationships and design requirements, then measure and verify native editable slides."
policy:
  allow_implicit_invocation: true
