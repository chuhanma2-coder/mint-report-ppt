interface:
  display_name: "Mint Report PPT"
  short_description: "原生PPT协作编辑，最终发布只读HTML"
  brand_color: "#00866A"
  default_prompt: "Use $mint-report-ppt to create my management-report PPT from the supplied sources. Internally produce and validate an executable Director Plan, then render readable native slides, test materially different complete compositions before splitting, and require independent executive visual acceptance before delivery."
policy:
  allow_implicit_invocation: true
