#!/usr/bin/env node
console.error("finalize-report-html.mjs was retired in mint-report-ppt v0.3. Use publish-report-html.mjs final.pptx output.html. The publisher reads the actual final PPT pages and never synchronizes edits back to IR or HTML.");
process.exit(2);
